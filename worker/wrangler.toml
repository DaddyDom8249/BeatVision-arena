name = "beatvision-provider-arena"
main = "src/video-fallback-gateway.ts"
compatibility_date = "2026-09-09"

[vars]
ALLOWED_ORIGIN = "https://daddydom8249.github.io"
LANGUAGE_PROVIDER_MODEL = "openai"
LANGUAGE_PROVIDER = "pollinations"
LANGUAGE_PROVIDER_URL = "https://gen.pollinations.ai/v1/chat/completions"
IMAGE_PROVIDER = "pollinations"
IMAGE_PROVIDER_MODEL = "flux"
VIDEO_PROVIDER = "pixazo"
PIXAZO_LTX_RESOLUTION = "720p"
PIXAZO_LTX_DURATION = "5"
PIXAZO_LTX_FPS = "25"
# HF fallback must use an actual image-to-video provider/model pair.
# Wan2.2-TI2V-5B was exposed by the selected HF route as text-to-video,
# which caused the observed fallback 503. Use the dedicated Wan2.1 I2V model.
HF_VIDEO_MODEL = "Wan-AI/Wan2.1-I2V-14B-720P"
HF_VIDEO_PROVIDER = "fal-ai"

# Pollinations remains the language, image and audio provider.
# Free Pixazo LTX is the primary motion provider.
# Shotstack Sandbox is the real MP4 assembly layer.
# Hugging Face remains an optional secondary motion fallback.
#
# Secrets expected by the worker:
# wrangler secret put GATEWAY_TOKEN
# wrangler secret put LANGUAGE_PROVIDER_TOKEN
# wrangler secret put IMAGE_PROVIDER_TOKEN
# wrangler secret put AUDIO_PROVIDER_TOKEN
# wrangler secret put PIXAZO_API_KEY
# wrangler secret put SHOTSTACK_API_KEY
# wrangler secret put HF_VIDEO_TOKEN
#
# SHOTSTACK_API_KEY must be the Shotstack Sandbox/Stage key.
